# this is an auto generated file. This will be overwritten
query BatchGetRatings($recipe_ids: [String]) {
  batchGetRatings(recipe_ids: $recipe_ids) {
    updated
    value
  }
}
query GetMenu($year_week: String!) {
  getMenu(year_week: $year_week) {
    updated
    value
  }
}
query GetRating($recipe_id: String!) {
  getRating(recipe_id: $recipe_id) {
    updated
    value
  }
}
query BatchGetRecipes($query: String) {
  batchGetRecipes(query: $query) {
    uri
    label
    image
    source
    url
    yield
    dietLabels
    healthLabels
    cautions
    ingredientLines
    ingredients
    calories
    totalWeight
    totalTime
    totalNutritens {
      label
      quantity
      unit
    }
    totalDialy {
      label
      quantity
      unit
    }
  }
}
query BatchGetRecipesWithRating($query: String) {
  batchGetRecipesWithRating(query: $query) {
    recipe {
      uri
      label
      image
      source
      url
      yield
      dietLabels
      healthLabels
      cautions
      ingredientLines
      ingredients
      calories
      totalWeight
      totalTime
      totalNutritens {
        label
        quantity
        unit
      }
      totalDialy {
        label
        quantity
        unit
      }
    }
    rating {
      updated
      value
    }
  }
}
